import { GiftedChat } from 'react-native-gifted-chat';
const Firebase = require('Firebase');

class ChatContainer extends Component {
  constructor(props){
    super(props);
    this._messageRef = New FireB
  }
}
