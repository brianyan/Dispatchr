language: ruby
rvm:
  - 2.2.2
env:
- DB=postgresql
# services:
#   - redis-server
script:
    - cd dispatchr-ruby
    - RAILS_ENV=test bundle exec rake db:schema:load --trace
    - bundle exec rake db:test:prepare
    - bundle exec rspec spec/
before_script:
  - cd dispatchr-ruby
  - cp config/database.travis.yml config/database.yml
  - psql -c 'create database dispatchr-ruby_test' -U postgres
